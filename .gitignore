*.iml
.gradle

# IDE state and scratch planning documents: neither belongs in a published repository.
.idea/
.artifacts/
/local.properties
.DS_Store
# Every module has one, not just the root — a leading slash would match the root only.
build/
/captures
.externalNativeBuild
.cxx
local.properties

# CLAP weights: 408 MB, fetched from Hugging Face on first use and kept in ~/.aimusic/models.
# Too large for a git repository, and not this project's to redistribute.
*.onnx

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log
